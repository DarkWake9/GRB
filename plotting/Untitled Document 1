IGNORE THE WHITE FILTER ALWAYS(REMOVE IF NEEDED)
GIVE IMPORTANCE TO THE FILTER WITH MORE POINTS(Ex: R r R' r' Rc I i have most points so don't remove if not higly outlayed;
Ex: UVW, UVM have very few points so if outliers remove them)
